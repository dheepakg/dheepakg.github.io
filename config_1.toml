baseurl = "/"
title = "Dheepak's blog"
languageCode = "en-us"
paginate = "10" # Number of posts per page
theme = "mainroad"
enableEmoji = true

[Author] # Used in authorbox
name = "Dheepak Govindaraj"
bio = "" # Fill in some bio


[Params]
subtitle = "" # Deprecated in favor of .Site.Params.logo.subtitle
description = "Dheepak's personal site" # Site description. Used in meta description
copyright = "Dheepak Govindaraj" # Footer copyright holder, otherwise will use site title
opengraph = true # Enable OpenGraph if true
schema = true # Enable Schema
twitter_cards = true # Enable Twitter Cards if true
readmore = true # Show "Read more" button in list if true
authorbox = false # Show authorbox at bottom of pages if true
toc = true # Enable Table of Contents
pager = true # Show pager navigation (prev/next links) at the bottom of pages if true
post_meta = [
    "author",
    "date",
    "categories",
    "translations",
] # Order of post meta information
mainSections = [
    "post",
    "blog",
    "news",
] # Specify section pages to show on home page and the "Recent articles" widget
dateformat = "2006-01-02" # Change the format of dates
mathjax = true # Enable MathJax
mathjaxPath = "https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.6/MathJax.js" # Specify MathJax path
mathjaxConfig = "TeX-AMS-MML_HTMLorMML" # Specify MathJax config
googleFontsLink = "https://fonts.googleapis.com/css?family=Open+Sans:400,400i,700" # Load Google Fonts
highlightColor = "" # Deprecated in favor of .Site.Params.style.vars.highlightColor
customCSS = [""] #["css/custom.css"] # Include custom CSS files
customJS = [""] #["js/custom.js"] # Include custom JS files


[Params.style.vars]
highlightColor = "#2d8be2" # Override highlight color
# Override font-family sets. Secondary font-family set responsible for pre, code, kbd, and samp tags font
# Take care of different quotes OR escaping symbols in these params if necessary
fontFamilyPrimary = "'Open Sans', Helvetica, Arial, sans-serif"
fontFamilySecondary = "SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace"

[Params.logo]
image = "" #"favicon-32x32.png" # Logo image. Path relative to "static"


[Params.sidebar]
home = "right" # Configure layout for home page
list = "left" # Configure layout for list pages
single = false # Configure layout for single pages
# Enable widgets in given order
widgets = ["search", "recent", "categories", "taglist", "social"]
# alternatively "ddg-search" can be used, to search via DuckDuckGo
# widgets = ["ddg-search", "recent", "categories", "taglist", "social", "languages"]


[Params.widgets]
recent_num = 5 # Set the number of articles in the "Recent articles" widget
tags_counter = false # Enable counter for each tag in "Tags" widget


[Params.widgets.social]
# Enable parts of social widget
twitter = "dheeepakg"
telegram = "dheeepak"
github = "dheepakg"


[frontmatter]
date = ["date", "publishDate", "lastmod"]
expiryDate = ["expiryDate"]
lastmod = [":git", "lastmod", "date", "publishDate"]
publishDate = ["publishDate", "date"]
